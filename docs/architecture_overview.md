# Architecture Overview

This project demonstrates a Snowflake ETL validation workflow using:

- AWS DMS
- Snowflake stages
- Parquet files
- Infer schema
- COPY INTO commands
- Validation queries
- Scheduled tasks

The workflow validates successful ingestion and downstream processing timing dependencies.
